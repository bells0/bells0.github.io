const cards=[...document.querySelectorAll('.card')];const q=document.querySelector('#q');function filter(){let n=0;const s=q.value.trim().toLowerCase();cards.forEach(c=>{let ok=c.dataset.search.includes(s);c.hidden=!ok;if(ok)n++});document.querySelectorAll('section').forEach(s=>s.hidden=![...s.querySelectorAll('.card')].some(c=>!c.hidden));document.querySelector('#shown').textContent=n+' 张'}q.addEventListener('input',filter);filter();